# ロードマップ ― turboR ショーケース版の作業計画

本書は「BattleshipProtoR v0.2.0 の土台の上で、turboR の実力誇示に向けて何を・どの順で作るか」の
作業計画。設計の狙いは [次版設計メモ](次版設計メモ_CPU重VDP軽の演出と1943ギミック.md)、
性能の一次資料は [性能と高速化.md](性能と高速化.md) を参照。本書は「その狙いを、この土台の
実測値の上でどう実行するか」だけを書く。

---

## 0. 出発点の実測（2026-09-11・引き継ぎ直後のクリーンビルド）

`make clean && make` は警告のみで成功、openMSX（C-BIOS MSX2+ / Z80）でタイトルまで起動を確認。

| 枠 | 使用 / 上限 | 残り |
|---|---|---|
| 常駐コード bank0–2 | 21,049 / 24,576 B | **3,527 B (14%)** |
| `hot_ram`（page3 RAM 実行枠） | 5,199 / 5,312 B | **113 B** |
| ROM | 256KB / 32 バンク | **空き 14 バンク = 112KB** |
| bank9–15 | タイトル YJK 画 54KB が占有 | ― |

**常駐 `_CODE` はモジュール別に以下（`build/rom.map` 実測）**:
scene_stage 5,693 / entity 3,618 / sound 2,415 / vdp 2,118 / fire 1,772 / scroll 1,085 /
sprites 1,058 / player 687 / hud 506 / ship_aag 489 / input 395 / その他 1,103 B。

---

## 1. 引き継ぎ時点で判明した構造的な問題

### 1-1. ★最重要: 一番熱いコードが、一番遅い側に落ちている

§4-3 の RAM 実行（`ramexec.c`）が高速化するのは **page1 = 0x4000–0x7FFF の 16KB だけ**。
常駐は 24KB あるので、**0x8000–0x91DB の約 4.5KB はカートリッジ ROM フェッチのまま**であり、
そこに居るのが実際には次の面々である（`build/rom.map`）:

| 番地 | 中身 | 実行頻度 |
|---|---|---|
| 0x8C07 | `stage_update`（1,330 B） | **毎フレーム。ゲームループ本体** |
| 0x8131 | `burn_add`（〜1,836 B） | 破壊イベント毎 |
| 0x885D | `stage_init`（938 B） | 面開始 |
| 0x9149–0x91DB | `__mulint` / `__divuint` / `__divsint` / `__moduint` | **毎フレーム多数回** |

どの関数が 0x8000 を越えるかは `Makefile` の `RESIDENT_RELS` の並び順で決まっているだけで、
**熱さでは選ばれていない**。docs の「R800 の ROM フェッチは RAM 実行の約 3.8 倍遅い」が正しいなら、
ここは演出予算に直結する取りこぼしである。**ただし本件は未測定の仮説であり、実機計測で確かめてから
直したと言うこと。**

### 1-2. 派手演出を置く RAM 枠が、もう無い

`hot_ram` の残り 113 B、常駐の残り 3,527 B。設計メモの A〜F（パレット・歪み・弾幕・アフィン・粒子）は
すべて「RAM 常駐の重い計算」なので、**現状は入れる場所が無い**。演出コードを演出ごとに RAM へ
載せ替える「エフェクト・オーバレイ・ローダ」（`hot_load` のシーン単位版）が前提として要る。

### 1-3. turboR 性能を検証する手段（決定済み）

openMSX 21.0 には `turbor.xml` があるが turboR システム ROM が未設置のため、この Mac では
**Z80 / MSX2+ でしか動かせない**。
→ **方針決定: 正しさは openMSX（Z80）、turboR 性能は実機 turboR ＋ `make DEBUG_PROF=1`（S1990
タイマの区間計測 `PF_*`）で測る。** 性能に関わる変更は **必ず前後の A/B 実測をセットで残す**。

### 1-4. 小さな不整合（順次片付ける）

- `config.mk` の `ROM_SIZE = 0x20000`（128KB）は死んだ値。実体は `tools/rompack.mjs` の
  `ROM_SIZE = 0x40000` にハードコード＝真実が二重。
- `ARCHITECTURE.md` が参照する `HANDOFF §1/§2/§7` は本リポジトリに存在しない（前作の文書）。
- `docs/仕様書.md` は旧作の仕様のまま。~~タイトル画も「零の咆哮 改」のまま。~~ → 2026-09-16 に「真 零の咆哮」のタイトル画へ差し替え済み。
- プロジェクト側 `CLAUDE.md` が未作成（監査項目・openMSX 運用・バージョン保持ファイル一覧）。

---

## 2. 作業計画

### TODO 一覧（2026-09-15 時点。詳細は各項目の節）

**次にやる（順番はユーザー指定）**
1. **B4 大型機の編隊（中ボス）**（相談しながら） — 最終面の「2倍拡大の帯」を小さめの機体で使う。最終面の驚きを残すため 2×2 まで（§P2-B）。
2. **A 系（相談しながら）** — A1 編隊ウェーブ＋POW＋時限武器 / A2 道中の中型目標 / A3 対艦ボム。まだ選んでいないもの: C 固有攻撃の予告（§P2-B）。
   ★**パワーアップ v1（2026-09-16, 実機テストプレイ待ち）**: 海モードに1機だけ銀＋赤帯の敵機(まっすぐゆっくり降下・撃ってこない)。
   落とすと増槽(16x16)が海と一緒に流れ、取ると段階+1(最高で取ると高得点1000)。段階1〜3は太い弾を縦/左上/右上へ3発・
   発射間隔 6→10、威力は半分単位で 2/4/5/6(耐久は半分単位。実機評価「簡単すぎる/5面が柔らかい/最終面が豆腐」を受けて面別に: 主砲40〜80・大型AA14〜26・小型AA8〜14、最終面の弱点40/60)、
   色は 1=赤で先端白 / 2=白で根元赤 / 3=全部白＋貫通(同じ相手には1回)。ミス・新規ゲーム・最終面の開始で通常へ。
   増槽の出現と取得はオーバレイ(`ovl_power.c`)。常駐の当たり判定へ書いたら SDCC の割付が崩れて 800B 膨らんだため。
   枠: 常駐 残り1288B(DEBUG_PROF 291B) / ovl.bin 残り731B / hot.bin 残り73B / ovl6.bin 残り46B。

**やめたもの**
- ~~B3 雲の層~~（2026-09-15 取りやめ）。★**1〜5面を低空飛行にしているのは、そもそも雲を描かずに済ませるため**（ユーザーの設計意図）。
  スプライトの網目雲 32x32 を試作したが「觔斗雲のようでしょぼい」と却下(`bf32529` → `ea7bc21` で取り消し)。
  帯ごとにパレットを白くする案は横縞になり雲に見えない。海面に雲の影だけを落とす案も含め、1〜5面に雲は入れない。

**中ボス(B4)の検証(2026-09-16 着手)**
- 別AIからの提案(面ごとに技術の見せ場を1つ: 1面=64方向の回転 / 2面=拡大縮小 / 3面=背景の潜水艦＋横スクロールのうねり /
  4面=パレット補間の霧 / 5面=背景弾幕)。実現可能性の確認結果:
  - **できる**: 1面(128B/フレームのパターン差し替え=0.63ms) / 2面(影は16x16に。32x32の背景影は5ms/フレームで重い) /
    3面(96x40の潜水艦を16フレームに分けて描く=0.6ms/フレーム) / 4面(パレット補間は無料)。
  - **数字が合わない**: 5面の背景弾幕100発(6x6の弾は描画＋消去で36B。100発=3600B=17.6ms/フレーム)。24〜32発、または 4x4 で60発。
  - **前提**: スプライトのパターン枠は満杯(海の区間だけ砲身32枠・津波20枠が空く=借りる)。
    オーバレイ8KBも満杯(海の区間は主砲の弾幕2172Bが要らない＝海の区間用オーバレイに入れ替えれば約2.9KB空く)。
- **検証ROM**: `make clean && make HSTEST=1`(bank22, `scene_hstest.c`)。走査線の途中で R#26/R#27 を書き換えられるかを見る。
  **2026-09-17 実機turboRで確認: 帯ごとに横へずらせる。ただし分割の間隔は 29 行(帯6本)までが安全で、22 行で怪しくなり 17 行では 2 本しか効かない**
  (openMSX は 14 行でも全部効くので判断材料にならない)。衝撃波の帯も 12→26 行へ広げた。
  ★R#26 は 1画面モードでは 0..31。bit5 を立てるとページが変わって画面が消える(踏んだ)。

**中ボス共通の決まり（2026-09-19。面ごとに見せ方が違うのは駄目=実機で指摘）**
- 被弾: 当たった機体だけ **1 フレーム**白く光る＋火花＋音。光った後 3 フレームは光らせない(当て続けてもチカチカ)。影は変えない。
  最終面のボスも同じ(以前は光りっぱなし)。「もっと速く元の色に戻して」とユーザー指摘(2026-09-19)。
- 突進/降下の予告: 12 フレーム白く明滅。影は変えない。
- 手負い(耐久半分): その機体のエンジンが燃え(爆発を繰り返す)、動きが速くなる。
- 撃墜: 爆発＋点数。最後の1機で残り時間ボーナス＋メガクラッシュ1回。
- 影: どの場面でも同じ色・同じ形。
- 中ボス戦の間は縦スクロールを止める(全面共通=ユーザー判断)。海の波の動き(SEA13)は止めない。
- 中ボスが出る瞬間に、残っている敵機・敵弾・爆発を消す(5面は拡大で仕掛けが見えるとユーザー指摘。全面で揃える)。
- ★面の流れ(2026-09-20 ユーザー指定): **海1 → フェードアウト → 中ボス → 海2 → フェードアウト → 警報「敵艦発見」→ 戦艦**。
  - 海1・海2 はどちらも約5秒(中ボスを入れる前の海モード1回分)。中ボスが終わったらカメラを 144px(16の倍数=海の模様が継ぎ目なく続く)巻き戻して海2 を作る。
    海2 の間は海の曲(track7)に戻す。銀の敵機(増槽)は面に1機のまま(ユーザー判断)。
  - フェードアウト: いきなり消すのは唐突(ユーザー指摘)。敵機・敵弾・爆発を 8 フレームずつ 淡灰→中灰→海の明→海の地 と沈めて消す(ovl_power.c)。
    その間は撃たず、影も消し、新しい敵は湧かない。画面に残っている増槽は取ったことにする(取り逃しで中ボスの間に絵が化け、撃破後に現れていた)。
  - 警報: 曲を止め、2音のサイレン(track10)と、毛筆の大きな「敵艦発見」＋小さな ENEMY FLEET SIGHTED(tools/gen_alert.py → bank19 が表示リングに描く)。
    2.5 秒(割込みで数える)で文字を海から引き直して消し、艦へ。警報の間は縦スクロールと海の塗り直し・メガクラッシュを止める。
  - 最終面: 無音で始め「敵大将発見」＋ENEMY FLAGSHIP SIGHTED の警報(サイレンのみ)→ **電文**→ **投棄**→ イントロ付きの曲と
    今までどおりのボス登場(曲の頭に合わせる)。
    - 電文(ユーザー案): サイレンを止めた無音の中で「我、敵総大将ヲ発見セリ / 乾坤一擲ノ決戦ヲ敢行ス」を**一文字ずつ**(5フレームごと)音を立てて出す。
      1文字 18x18 を毛筆で焼いて bank19 へ(gen_alert.py)。前景(vdp_wait_frame)で回すのでゲームのフレームは進まない。
    - 投棄(ユーザー案A): 電文のあと、持っている増槽と爆弾を自機が捨てる(威力0の自機の弾として落とす)。捨てた瞬間に弾の段階とボムを 0 にする
      =「黙って 0 にする」のをやめ、捨てたから弱くなった、という順番にした。
- 2026-09-20 実機の指摘で直したもの:
  - 4面(双子): 片方を落としたら残りの1機が画面全体で暴れる(下の機は自機の近くを飛ぶので無敵でもないと落とせなかった)。
    下の機が残ったら上の機(表A)の役に移して上へ抜け、分割をやめて自機を直接狙う。下半分に居たら自機の真上の上空へ引き返す。
    手負いで旋回が速いと、絵の読み込みが上の機ばかりに回って下の機の向きが一気に変わっていた → 両方変わったら交互に読む。
  - 5面(P-61): 128x128 は大きすぎて最終面のボスの印象が薄れる → 48x48 の絵(3x3 マス)を拡大して 96x96。スプライトも 22→15 枚以下。
  - 2面(空母): 主砲の耐久 48→36(小さな5インチ砲。左前の砲は停泊機の列の奥で弾が届きにくい)。
  - **中ボス戦の最中にミスすると中ボスが「空中分解」して見えた**: ミスの処理に中ボスの後片付けが無かった。
    後片付け(mb_restore_hw)を1か所にまとめ、ミスからも呼ぶ: 拡大(MAG)・絵の表B・借りたパターン行・3面の横ずれ/MSK/海の除外・
    末尾の枠の停止マーカ指定(g_spr_hide_to)を戻し、スプライトを全部隠し、背景に描いたもの(5面の弾幕とHUD/3面の駆逐艦)を海から引き直す。
  - 自機の影が**左端に出る**: 影を右下へずらすとき X を u8 に丸めていたので、右端で 255 を超えると回り込んでいた(最終面は離す量が大きい)。
  - 最終面の壁の音: 16フレームごとの打ち直しでは 0.8 秒の間に1〜2回しか鳴らず「がっ」1回に聞こえた → 4フレームごと。
  - パワーアップ段階の表示(ユーザー要望): 画面下中央。最終面の投棄では増槽→爆弾の順に**1つずつ**落とし、そのたびに
    段階/残数とアイコンを1つ減らす(gen_planes.c の jettison。前景なので自分で HUD を描き直す)。
    ★**山形(階級章)1枠**に作り直した(増槽アイコンを段階ぶん並べる方式から): 実機で「増槽の絵が爆弾に見える」
      「枠を3つも使う必要があるのか」と指摘された。絵は山形3本を**焼いたまま**にして、出す本数は
      **行別カラーで決める**(出さない山の行を色0=透明)。段階が変わったときに 16B 置き直すだけ＝**枠は常に1つ**。
      ★パターンを毎回組み直す実装も試したが、常駐が 166B 増えてデバッグROM(DEBUG_FPS=1)が入らなくなった。
        色表だけで済ませれば 55B で収まる。**常駐の増減は必ずデバッグROMもリンクして確かめる**(通常ROMだけ見ていて踏んだ)。
      ★**通常弾を「1本」として数える**(実機で指摘): 段階を取った数だけ出すと「死なずに3つ取った状態」を
        表しているようで違和感がある。通常=1本 / 三方向=2本 / 威力2.5倍=3本 / **最終(貫通)=4本**。
        4段階目を分けるのは**貫通**があるから(弾が敵を抜けて後ろにも当たる＝戦い方が変わる)。
        1本=3行＋間1行で 16 行にちょうど4本入る。
      色は弾と同じ考え方(通常=赤、上げるほど白が増え、最終は全部白)。絵の枠は死枠だった SPR_BLOCK(パターン0)を転用。
      ★色表(4段階×16B)は**常駐に置かず**、面の準備で bank16(ship_render.c)が常駐の RAM へ流し込む
        (常駐のコード枠を 48B 節約。デバッグROM(DEBUG_FPS=1)が入らなくなるため)。
      ★**枠は HUD ではなく ent_draw_all の最後(=最低優先)**に置く。HUD の枠(優先度が高い)に置いたら、
        中ボス戦で1走査線8枚を超えたときに**アイコンではなく弾のほうが消えた**(実機で指摘)。
        混んだらアイコンが落ちる＝弾は必ず見える。出さない段階(g_pwr=0)では枠を1つも使わない。
      ★5面の中ボスの間はスプライトが拡大(MAG)なので出せない。ボム残数と同じように**背景へ描く**
        (ovl_mb_p61.c の hud_bg。焼いてある絵を VRAM から読み、出さない山の行を落として置く)。
      ★最終面の投棄は**前景**(ゲームのフレームが進まない)なので ent_draw_all が回らない。gen_planes.c が
        空いている先頭の枠(g_spr_used)へ自分で置き直し、その後ろに停止マーカを書く(段階が1つずつ減って見える)。
  - 常駐が満杯になったので、面の準備の配置処理(対空砲の初期化・主砲4基/停泊機の配置・面別の耐久/発砲の表)を
    **bank30(coldsetup.c)** へ移した。常駐の空き 0B → 553B。中身は RAM だけを触るのでバンクで動かせる。
    ★**ポインタの実体はバンクへ移してはいけない**: 主砲の発砲スクリプトの表を移したら、砲台が持つポインタが
      バンクの窓(0xA000〜)を指し、窓が既定へ戻った瞬間に中身が変わって主砲が撃たなくなった(実機で「2面で全く撃ってこない」と指摘)。
      バンクへ移してよいのは「値」だけ(耐久など)。
  - **最終面のボスが左へ行ったきり動かなくなる**(実機で指摘): 横移動の目標を「左3のコマが収まる範囲(70〜170)」から
    選んでいたが、傾きのコマは角度ごとに翼の張り出しが違い、**左2 のほうが左3 より左へ出る**。目標へ近づいて
    左3→左2 に戻る途中で clamp_cy() に押し戻され、cx が目標に着かない=`cx == tx` にならず次の目標も選ばれない、で固着した。
    目標の範囲を**戦闘で出る全コマ(通常＋傾き6枚)の共通部分**(BOSS_XL/BOSS_XR=88〜166。gen_boss.py が算出)にして解消。
    加えて保険として、目標へ着けないまま 24 フレーム動けなかったら目標を捨てる(絵を作り直したときの再発止め)。
  - **中ボス戦でメガクラッシュを使うと中ボスが消えた**(実機で指摘): 津波は**32枚すべての色表**を水の色で上書きする。
    HUD(hud_colors)と敵(ent_spr_cache_inval)は戻していたが、**中ボスの色表を戻す者がいなかった**。
    中ボスは色を「絵が変わったときだけ」書くので、波の色(海と同じ)のまま残り、機体が海に溶けて見えた。
    常駐→オーバレイのフラグ `g_mb_recol` を足し、津波が終わったフレームで立てて各中ボスが塗り直す
    (1/2/5面=coldirty、4面=paint＋影4枚、3面の駆逐艦は毎フレーム書いているので不要)。
  - **3面(駆逐艦)でボムを使うと艦が消えた**: 津波は稲妻を消すために背景を世界の正本から引き直す
    (scroll_repaint_all)。この艦は**リングへ直接描いてある**ので一緒に消えていた。g_mb_recol を受けたら
    登場と同じ手順(2行ずつ)で描き直す＝嵐の中からもう一度現れる絵になる。
  - **5面の中ボス戦でボムが使えないことを演出で示す**(ユーザー指示: 「プログラムの仕様上」はプレイヤーに関係ない)。
    設定は**電探妨害**(相手は夜戦 P-61=電探機)。背景の HUD でボムの棒を暗い単色に落とし、右に `JAMMED` を出す。
    押したら白く光らせ、砂嵐の音(SFX_EFIRE)を数フレームおきに返す(押したことは必ず伝える)。
  - パワーアップのアイコンは**枠を1つ予約**する(g_spr_limit を1減らす)。最低優先のまま「枠が無くて出ない」時間が
    長いと実機で指摘されたため。走査線が混んだときに落ちるのは従来どおり(弾を優先)。
  - **敵弾が画面に貼り付いて見える**(実機で指摘): 敵弾が艦(背景)の縦スクロールに追従していたため、上り/下りで
    見かけの速度が変わり、打ち消し合うと完全に静止した(2面の艦の区間で実測: 真下へ速度2×スクロール2)。
    ★これは**前作で直した規約が戻っていた**もの: BattleshipProtoR af85108(8/27)で「敵弾はスクロール非依存=
      画面を静止画とみなして発射時の速度で一定に進む」とユーザー指示で直したが、後の高速化 3b6249e(9/1)で
      `e->y -= g_scroll_dy` が再び入り、本作はその状態を引き継いでいた(bh_bullet の見出しコメントだけ正しいまま)。
    ★いったん「止まる場合だけ補正」で対処したが、規約そのものが違うとユーザーに指摘され撤回。
      **全敵弾(通常弾・信管弾・合体弾の予告・CPU 弾幕)から艦追従を外し、g_scroll_dy 自体を削除**した
      (使う者がいなければ誤って戻せない)。検証: 敵弾 3020 組を追跡、止まった弾 0。
  - **2面の中ボスが上昇中でも自機の下に描かれる**(実機で指摘): 中ボスはふだん末尾の枠(最低優先)に置くので、
    自機より必ず後ろに出る。高い所に居る間(上昇中＋高度が自機より上)は **HUD の直後の枠**へ置き、
    エンティティをその後ろへ下げる(常駐↔オーバレイのフラグ g_mb_front。g_spr_base を中ボスのぶんずらす)。
    上昇中は胴体の当たり判定も外した(自機の上を通り抜けるので当たらないのが自然)。
    ★最初の実装は**オーバレイが自分で先頭の枠を計算**していた。常駐が g_spr_base を決めた後に、オーバレイが同じ
      フレームで向き/枚数(g_mb_n)を変えるので先頭がずれ、**HUD の枠(ボム棒・アイコン)へはみ出して書いた**
      (実機で「ボム棒と影が変な色、アイコンが消えた」)。前回のテストでも先頭がマイナスの値で出ていたのに見落とした。
    ★直し方: 使ってよい枠 [g_mb_s0, g_mb_end) を**常駐が毎フレーム確定**し、オーバレイはその外へ書かない
      (余れば画面外、足りなければそのフレームだけ末尾の影を落とす)。範囲が変わったら(先頭でも末尾でも)
      g_mb_recol で色を置き直させる。手前のときは先頭が動かず末尾だけ伸びるので、末尾を見ないと
      伸びたぶんの影が前の持ち主の色のまま出た。
    ★検証(2面の中ボス戦 1521標本、うち手前 793): HUD 破損 0 / ボム棒の色違い 0 / アイコン消失 0 /
      影の色違い 3654 中 2(2標本続いたもの 0＝1フレーム未満の書き換え途中)。1面・3面も同じ検査で異常なし。
  - **撃沈で戦艦が「ちょん切れて」消える**(実機で指摘): 水面が画面の上端の **12 ドット手前**に着いた時点で
    演出を切り上げ、残りをまとめて海へ戻していた。そのとき**まだ見えている 12 ドットぶんの艦**が 1 フレームで
    消え、さらに水面の行だけが片付けの範囲から外れて 1 本残った(openMSX で再現: 8行→1行に飛び、その1行が残留)。
    上端に着くまで沈め切るようにした(見えていない行だけをまとめて戻す)。直後: 8行→2行→1行→0 と滑らかに沈む。
  - **4面の中ボス(He 111×2)を落としても分からない／海に溶ける**(実機で指摘):
    ・撃墜の爆発はエンティティで出していたが、中ボス戦の間はエンティティの枠が 4 枚ほどしか無く、ほとんど出なかった。
      機体そのものは何も変わらず 40 フレーム後に消えていた。→ 前半 24 フレームは機体を赤と白で激しく明滅させ弾を止め、
      後半 16 フレームは**自分の枠で**本体のマスを爆発の絵(4 コマ)に替えて火の玉にする(put_fire)。消える瞬間にもう一発。
    ・明るい色 4(4,4,5) は 4面(朝霧)の海の地色 1(3,4,5) とほぼ同じだった。候補を並べてユーザーが選んだ 6(6,6,5)
      淡い温灰に替えた(暗い分割迷彩 14 はそのまま)。霧から現れる途中の灰(色 4)は演出なのでそのまま。
  - **最終面の投棄で、ぽっぽっと鳴っているのにボムもパワーアップ表示も減らない**(実機で指摘):
    ・アイコンを**別の枠に足して**描いていたので、元のアイコン(捨てる前の段階・優先度が高い)が上に残り、減らないように見えた。
      → ent_draw_all が最後に描いたアイコンの枠(g_spr_used-1)の色表だけを書き換える。
    ・「音 → 25 フレーム待つ → 表示を減らす」の順で毎回1拍遅れていた。捨てた物(エンティティ)もゲームが動き出すまで出なかった。
      → 減らす → 表示を直す → 音と同時に捨てた物がその場から落ちていくのを前景で描く(drop_fg)。
    検証: 音と同時に 山4→3→2→1、ボム棒 3→2→1→0、落下物の Y が毎フレーム 3 ずつ下がる。アイコンは常に1枚。
  - **5面の中ボス戦で宙返りすると、その後の自機が緑一色になる**(実機で指摘): 5面の中ボスの間は HUD を背景に描くので、
    宙返りの4枚は枠 0..3 に置き(ovl_rot.c)、エンティティも枠 0 から詰める。ところが宙返りの後に捨てる色キャッシュが
    HUD_SLOTS(9)から先だけだったため、枠 0..3 は「自機の色を置いた」ままと思い込み、宙返りの単色(3)が残った。
    → 色キャッシュは枠 0 から捨てる(中ボスの枠の範囲が変わったときと、中ボスの後片付けも同じ理由で 0 から)。
  - **撃沈中、艦の途中(船首寄り)に横長の抜けた帯ができる**(実機・WebMSX で指摘): 撃沈は水面より下を「表示リング」と
    艦バッファ B の両方で海に塗る。リングへ書いてよい行を cam から「cam&0xFFF0 から 256 行」と推定していたが、
    カメラが上下したあとのリングは実際にはそれより 1〜2 段(16行)上を保持している。そのため水面の下の行が、256 上の
    **まだ画面に出ていない船首寄りの行**のスロットへ海として書かれ、スクロールでそこが降りてくると 12 行ほどの帯が抜けた
    (B は艦のままなので、リングだけが海)。→ スクロール側が描いた範囲 [drawn_top, drawn_bot] で判定する。
    検証(openMSX、全5面・艦の出現から20〜26秒で撃沈): 前は全面でリングと B の不一致が最大12行×約3秒続いた → 後は 0(5面の1標本だけ、描いている最中の上端1行)。
  - **宙返りの効果音と、宙返り中の制約の点検**(ユーザー要望):
    ・効果音: 引き起こしの合図が自機弾の音(仮)のままだった → 専用の SFX_LOOP「ぶーーーん」(tone B、周期 900→438=
      約124→255Hz へ上ずる、34 ISR フレーム)。常駐に余裕が無い(デバッグ版 残り9B)ので、tone B の3種(弾/被弾/宙返り)を
      同じ式「周期=始め+経過×傾き」の表にまとめた(弾と被弾の値は従来と同一)。結果デバッグ版は 残り94B に増えた。
    ・当たらない: 被弾処理(ent_player_hit)は無視していたが、**敵弾は当たり判定で消していた**(下を抜けず吸われる)。
      エンティティの弾・CPU 弾幕・5面中ボスの弾とも、宙返り中は判定そのものをしない。
    ・ボム: 宙返り中の B 単押しでメガクラッシュが出ていた → 宙返り中は B を受け付けない。
      逆(クラッシュ中の宙返り)はクラッシュの間シーンが先に戻るので元から出ない。通常弾も元から撃てない(player.c)。
    検証(openMSX): 宙返り中に B 単押し→ボム残3のまま、宙返り後の B 単押し→残2。宙返り中は弾の音が鳴らない。
      PSG ch B の周期が 900 から 14 ずつ 438 まで下がる(=音程が上がる)。
    ・**「ぶーーーん」が全く聞き取れない**(実機で指摘): 124Hz〜・音量11 は本体スピーカーで出ない低さで BGM に埋もれた。
      → 224→486Hz、音量 15/10 を2フレームずつ交互(エンジンの震え)、0.77秒。openMSX の録音で、宙返りの間の音の大きさ(RMS)が
      BGM だけの区間の約2倍になり、主成分が 230→450Hz へ上がっていくことを確認。
  - 測定スクリプトの番地は build/rom.map から自動生成する(tools/gen_syms_tcl.sh)。常駐の番地はビルドのたびに動き、
    ハードコードしていたせいで別の変数へ書き込んで誤診する事故を数回起こした。
- 1面の Fw 200 にも影を付けた(迷彩の重ねを 6→2 に減らしてでも影を入れる=ユーザー判断)。
- ★**最終的に揃えるもの: 衝撃波**。3面・4面・5面の中ボスの間は衝撃波のゆがみが出ない(3面=うねりの帯とぶつかる / 4面・5面=オーバレイの枠)。
- ★5面の中ボスの間はメガクラッシュを使えない(津波自体が拡大を使う＋オーバレイの枠)。
  全部の中ボスがそろってから揃え方を考える(ユーザー: 全部そろえば解決策を思いつくかもしれない)。

**中ボス v1: 1面 Fw 200（2026-09-18 実機確認OK）**
- 海の区間で艦が見える手前(cam=艦+48)に出る。戦っている間は海を流したまま、カメラを 256 ずつ巻き戻す(リングも R#23 も不変)。終わったら 256 前送り。
- 経緯: 32x32 1機 →「しょぼい」→ 2機(左右の円を逆回り) →「迫力が無い、1機でよいので倍に」→ **64x64(16x16 を 4x4)**。
  単色は「いかにもMSX」、画面の行で陰影を付けると「旋回で色が変わる」と指摘 → **部位ごとの迷彩を向きごとに焼き、6マスだけ2枚重ね**(見本の2段目を選択)。
- 32方向×1024B(tools/gen_fw200.py → bank44〜47)。向きが変わるとオーバレイが g_mb_req に置き、常駐がフレームの終わりに 0xE700 へ読み、
  次のフレームでオーバレイがパターン(704B)・色・位置をまとめて書く。スプライトは末尾の最低優先枠に 16〜18枚(1行に最大6枚)。
- パターンは砲身・発艦中の小/中の艦載機の枠(計22)を借り、終わったら page0 の退避から書き戻す。オーバレイは ovl8(残り16B)。
- 動き: ずっと旋回は「せわしない」と指摘 → **突進型**。自機を見て8方向のどれかを決め、向き直り(ここだけ回転が見える・3方向弾)→ 一直線に突進(3px/f, 45f)。
  画面の下半分に居るときは自機の真上の上空へ引き返す(下端で自機に重なったまま止まるのを防ぐ)。胴体に触れると被弾。
- 耐久200(半分単位, 400 は1面には硬すぎた)、40秒で逃走。撃墜で 500点＋残り1秒10点＋メガクラッシュ1回。
- 突進の前に12フレーム止まって白く明滅(予告)。耐久が半分を切ると手負い: 片側の内側エンジンが燃え、向き直り倍速・待ち半分・突進 4px/f。
- ovl8 から増槽(ovl_power)を外した(中ボス戦の間は銀の敵機が出ない)。残り167B。ovl_midboss.c だけ --opt-code-size で作る。
- まだ無いもの: 背景の影 / メガクラッシュの当たり / 耐久と弾の調整。

**中ボス v1: 2面 PBY カタリナ（2026-09-19 実機確認OK）**
- B-25 案は「1面と何が違うの」と指摘 → 形も色も違う飛行艇(胴体の上の一枚の長い主翼・船形胴体・ブリスター・海軍の青灰色)に。
- 遊び: **高さ**。ふだんは低空(自機と同じ高さ, 43 ドット)で撃ってきて当たる。150 フレーム経つか 40 削られると上昇して逃げる。
  高い間(60 ドット)は当たらず、**機体を1色(淡い黄)で塗って当たらないことを示す**。自機と反対側の上空へ回り込み、白く明滅して降りてくる。
  ★最初は「高いのが常態・降りた時だけ当たる」にしたが、1〜5面は雲の無い低空で戦う設定と逆なので反転(ユーザー指摘)。
- 影は海面(32 ドット一定)。夕日で右下へ、高いほど離れる。低空でも 12 ドットずらす(0 だと機体の真下に隠れて見えなかった)。
- 高い間の絵は 30 ドットの荒い元絵を最近傍で引き伸ばす(最高で 2 倍)=ドットを荒くして「拡大」と分かるように(ユーザー要望)。
- 中ボスの間は自機の影を自機の直後に描く(shadow=2)。エンティティの枠が 5〜9 枚しか無く、最後に描く影が溢れて消えていた。
- 6段階の大きさ×16方向×1024B(gen_pby.py → bank48〜59)＋影16×128B(bank60)。オーバレイ ovl9(bank25, 入口は ovlhead8 を共用)。
  絵を VRAM に書く処理(mb_upload)と後片付け(mb_finish)は両中ボス共用で常駐へ。
- 耐久160(半分単位)、40秒で逃走。撃墜で 500点＋残り時間＋メガクラッシュ1回。手負いで右エンジン炎上・速くなる。

**中ボス v1: 5面 P-61 ブラックウィドウ（2026-09-19, 実機テストプレイ待ち）**
- 米の双胴の夜間戦闘機。**中ボスの間だけスプライトを2倍に拡大(MAG)して 128x128**(ユーザー案)。自機・弾は絵の表B(0x2000, R#6=0x04)に
  半分へ縮めた絵(2x2 画素の OR)を置き、拡大で元の大きさに見せる。縮める処理は読み込み直前に常駐が bank19(gen_planes の mag_table)を呼ぶ。
  スコア・残機・ボム残数は数字を半分にすると読めないので背景に描く(hud_draw を止め、エンティティは枠 0 から)。戦艦の区間で拡大を切って戻す。
- 遊び: 弾幕をかいくぐる。上に居座って左右に動き、機首を自機へ向けて2本の腕の渦巻きに背景の弾(最大48発, BGTEST で実測した差分描きの asm。
  20発では「弾数少ない」と指摘)。150 フレームごとに白く明滅して自機へ扇11発。手負いで弾幕が濃く速く。耐久240、45秒で逃走。
- 残機のアイコンとボム棒も背景に、HUD のスプライトと同じ絵・同じ色で描く(bgspr)。
- 絵は 9方向(真下±45°)×1KB を影のバンク 60/61/62 の後ろ半分へ分けて置く。影は重ねの直後に入れた(mb_upload 1回で書ける)。
- オーバレイ ovl12 は最後の空きバンク 3。枠のため衝撃波とメガクラッシュを外した。弾の表は 0xEB28、海のひな形は 0xBF00。
- ★gen_planes(bank19)で「0x80 >> i」の形が全行 0x80 になった(SDCC)。マスクを1ビットずつずらす形に書き直した。

**中ボス v1: 3面 駆逐艦（2026-09-19, 実機テストプレイ待ち）**
- フッドの護衛の英駆逐艦(トライバル級)。潜水艦案は「ゲームに潜水艦が出てこない」と指摘され変更。飛行機ではなく背景に描く艦。
- 絵(gen_dd.py, 176x24, 1行128B で bank60 の後半)を登場時に 2 行ずつ、VRAM の海と重ねてリングへ描く(スクロールは止まっている)。
- **動かすのは描き直しでなく、その帯(32..63 行)を走査線の分割で左右へずらす**(R#26/R#27)。ほかの帯は荒天のうねり(R#27 0..6)。
  左端 8 ドットは R#25 の MSK で隠す。分割表は ovl_dd_split(ovlhead11 の slot14)。艦の行は SEA13 から外す(g_sea_skip)。
- 被弾/予告の白は、パレット 9 番(甲板)を中ボスの間だけ艦専用(灰)にしてパレットで白へ(ovl_palette の OVL_DD)。
- 砲塔4基の砲身はスプライト(SPR_BARREL0)で自機を向く。1基ずつ撃ち、4回に1回は白く明滅して4基から3方向の斉射。
- 耐久280(半分単位)、手負いで煙突から黒煙・蛇行が倍速、40秒で嵐に消える。撃沈で下の行から海へ戻る。オーバレイ ovl11(bank63)。

**中ボス v1: 4面 He 111 ×2（2026-09-19, 実機テストプレイ待ち）**
- 双子艦の面なので2機(ユーザーのこだわり)。**106 行でスプライト表(R#5)と絵の表(R#6)を上下で切り替え**、上の帯=表Aに1機目、
  下の帯=表B(属性 0x7200 / 色 0x7000 / 絵 0x2000=page0 の 64..79 行)に2機目を、それぞれ 64x64 で描く(ユーザー案)。
- 2機は画面の中心について**点対称**(2機目の位置=反転・向き=+180°)。1機目の中心 y を 28..70 に留めるので分割線はいつも 106 行。
- 動き: 1機目が自機の点対称の位置を狙って 8 方向で突進(=2機目が自機の帯を突っ切る)。向き直りの途中で2機とも自機を狙って撃つ(弾が交差)。
  最初は霧の中から浮かび上がる(機体の色を霧の色→灰→本来の色)。片方を落とすと残りが怒る。45秒で上下へ逃げる。
- 絵の表Bは出現時に表Aを写す。その後の HUD の絵の書き換えは vdp_sprite_pattern が g_spr_patb で両方へ、宙返りは ovl_rot の OVL_TWIN 版が両方へ。
  メガクラッシュ中は絵の表を切り替えないので2機を隠す。分割表への足し込みは ovl_twin_shock(ovlhead10 の slot14)。
- 32方向×1024B(gen_he111.py → bank20〜23。テスト ROM の bank20〜22 は 29〜31 へ移した)。オーバレイ ovl10(bank26, 残り24B)。
  枠を空けるため ovl10 のパレット処理は4面の1行だけ・3面の稲光なし(-DOVL_TWIN, stage_grade.h の PAL_ONLY_STAGE)。
- 影(30 ドット=2x2, 右下へ 12)を追加(最初は朝霧を理由に省いたが実機で「影が無い」と指摘)。そのため重ねを最大2に(本体＋重ね＋影で18枚以下)。
  影は 32方向×128B(bank61)。PBY の影は常駐が 添字&31 で引くので16方向を2周に。
- 被弾は共通の決まりどおり当たった機だけ白く光る(一度「消えてちらつく」にして、面ごとに違うのは駄目と指摘された)。色は colbuf(0xBD00)に控えて戻す。
- **影は光らない・消えない・色も変わらない**(予告の白・霧の色・被弾のちらつきが影にも掛かっていて実機で指摘)。機体の色だけ帯ごとの色表へ直接書く。
- 4面の中ボスの間は衝撃波のゆがみを出さない: 分割表は ovl_twin_shock が「先頭=表A / 106 行=表B」の2本だけ組む。
  ovl_shock をこのオーバレイから外して枠を空けた(ovl10 の残り 1236B)。画面の揺れとヒットストップは残る。
- 常駐の残りは 47B。

**最終面の雲（相談中）**
- 高高度なので雲は下に見下ろす。ユーザー案: 小さい雲を背景に描き、8ドット単位で「濃い→薄い→濃い」と明滅させながらゆっくり動かす。
  2か所に描いて逆位相で明滅させる(クロスフェード)とさらに滑らか。制約: 最終面はパレットに空きが無い(自機専用の 8/10 番をボス帯だけ差し替える)、
  背景弾の消去で雲に穴が開く、`ovl6.bin` の残り 55B。試作するなら「明滅で跳ぶ」「クロスフェード」の両方を ROM に入れて実機で見比べる。

**決まっていて、条件がそろったら入れる**
- ~~**最終面の装備リセット**~~ — パワーアップ v1 で `stage_build` の最終面分岐に `g_pwr = 0` を入れた（ボムは元から0）。
- **津波の多重化** — 今と同じ大きさの波を2〜3個、同じ走査線に2つ乗らない間隔で続けて押し寄せさせる（§P1）。

**実機で見てもらって調整する**
- **撃沈シーン** — 長さと速さ。openMSX では撃沈中が 15〜20fps（炎と爆発が多い）で、撃破から結果画面まで約10秒。長すぎれば水面の速さ(`spd`)と誘爆の長さ(`BLAST_T`)を詰める（`ovl_sink.c`）。
- **面ごとの時間帯・天候の色** — 強すぎる/弱すぎる面があれば `tools/gen_grade.py` の表を直す（見本画像を先に見せる）。

**最終面の残件**（§P2 項目8）
- 弱点を壊した跡の見た目 / 撃墜時の爆発 / 難易度調整（弱点の耐久）。

**枠の残り（足す前に確認）**
- 常駐コード: 通常 1751B / DEBUG_PROF ビルド 約770B。
- オーバレイ: 通常面 `ovl.bin` 残り 1178B / 最終面 `ovl6.bin` 残り 55B（ほぼ満杯） / 撃沈 `ovl7.bin` 残り 5142B。
- hot.bin 残り 109B。

**記録**
- `docs/苦労と教訓.md` に撃沈シーンの教訓を足す（リングは 256 行周期なので 16 行単位に切り下げた範囲にだけ書く / 艦に貼り付いたスプライトと炎も一緒に消す /
  分割を止めたらスプライト表・パターン表・MAG を基準へ戻す / 表示の切替は塗り終えてから VBLANK でまとめて / 連続写真は結果画面まで通しで撮る）。

**P3 アイデンティティ**
- 名称(「真 零の咆哮」に決定)・~~タイトル画~~(差し替え済み)・`仕様書.md` の書き換え、CHANGELOG / VERSION 運用の開始。

### P0 — 基盤（演出を載せる前にやる）★現在ここ

1. **RAM 実行の適用範囲を、熱さで決め直す**（§1-1）。→ **(a) を実装し、実機 A/B で効果を確認済み（完了）。**
   実機 turboR 実測で **COMPUTE −941µs/frame (−9.8%)**、COLLIDE −10.8% / DRAW −8.9% / UPDATE −5.3% /
   AA −2.1%、CMDWAIT は不変。数表と但し書きは [性能と高速化 §3-A2](性能と高速化.md)。
   ★同時に **CMDWAIT ≈ 14.1ms/frame > COMPUTE 8.7ms** が実測され、**律速が VDP であること**が確定した。
   演出は必ず「重い計算・軽い出力」で作る、という本企画の芯が数字で裏付けられた。
   - **(a) 機構で直す（採用）**: §4-3 を **page2(0x8000–0x9FFF) にも拡張**し、常駐 24KB 全体を
     RAM 実行にした。出入口は `ramx_use_ram()`/`ramx_use_cart()` の1対に集約（page1 と同じ区間・
     同じ規律）。リンク順に依存しない恒久的な解。詳細は [ARCHITECTURE §3.9](ARCHITECTURE.md)、
     踏んだ地雷は [苦労と教訓 §5-1](苦労と教訓.md)。
     - openMSX で確認済み: `g_ramx_ok=1 / g_ramx2_ok=1`、`slotselect` でホット区間内のみ
       **0x4000・0x8000 が共に RAM スロット**、区間外は cart。130 秒の実プレイ回帰で破綻なし。
     - **未検証（実機のみ）**: 速度がどれだけ改善するか。`make clean && make DEBUG_PROF=1` と
       `make clean && make DEBUG_PROF=1 NO_RAMX2=1` の2本を実機で走らせ、`PF_*` の区間 tick を
       比較する。**この数字が出るまで「速くなった」とは書かない。**
   - **(b) 並びで直す（不採用）**: `RESIDENT_RELS` の順とファイル内の関数順を並べ替え、
     冷たいものだけ 0x8000 以降へ追い出す案。モジュールを足すたびに崩れるので恒久解にならない。
2. **エフェクト RAM オーバレイ機構**（§1-2）→ **完了**。
   ★**page2 を RAM 化している間、0xA000-0xBFFF が丸ごと遊んでいた**のを使う。§4-3 の page2 RAM 実行は
   空きセグメント seg5(16KB)を page2 へ割り当て、その**下位 8KB にだけ**常駐 bank2 の複製を置いている。
   page2 は 0x8000-0xBFFF なので上位 8KB が未使用のまま残る。ここへ演出コードを載せる。
   - **追加の RAM 消費ゼロ**(既に確保済みセグメントの空き) / **RAM 実行なので R800 で全速**
     (バンク実行の 3.84 倍) / **8KB まるごと**使える
   - 制約と作法は [overlay.h](../src/include/overlay.h)。要点は「ホット区間の中でしか呼べない」
     「オーバレイ側から data_read/bcall は不可」「常駐の関数/データは通常どおり呼べる」。
   - 最初の利用者＝CPU 弾幕の本体。**常駐 23726B → 22397B(+1329B)**、ovl.bin = 1491/8192B。
2-B. **常駐リクレイム（P0-2 の続き・都度）**: 演出を常時オンにしたところ `DEBUG_PROF` ビルドが
   常駐 24KB を 998B 超過した（rompack が検出）。面開始で 1 回しか走らない cold code を追い出して回収:
   戦闘機 8 方向の手続き生成（約900B）→ **bank19**(`gen_planes.c`)、弾幕の斉射/流し込み → オーバレイ。
   現況は **通常 21,516B（残り 3.0KB）/ DEBUG_PROF 24,448B（残り 128B）**。
   ★DEBUG_PROF が 128B しか余っていない。次に常駐へ足す前に、また何かを追い出すこと。
3. **フレームレート方針の確定**: 現状 30fps 固定（2 VBLANK 待ち）で 1 VBLANK 分を捨てている。
   **方針＝30fps 据え置きのまま、空き VBLANK を演出に全振り**（速度定数の再調整が不要でリスクが低い）。
4. **掃除**: `ROM_SIZE` 一本化、`HANDOFF` 参照の解決、プロジェクト `CLAUDE.md` 作成。

### P1 — 見せ場（設計メモ §2 の推奨順 A→B）

★③ 弾幕と ★A パレットエンジンは**完了・常時オン**（フラグを畳み済み）。到達点:
`raster.c`(R#19/FH の画面分割) → スプライト表2セット(R#5 切替で総数32の壁を破る) →
`curtain.c`+`ovl_curtain.c`(CPU弾幕をオーバレイで実行) → 当たり判定を `ent_player_hit` へ共通化 →
発射源を戦艦の主砲からの斉射に(設計メモ §4-3)。`ovl_palette.c`(被弾の赤染め/撃破の白フラッシュ/
海のシマー)。実測で同時生存20発・表示24発、パレットは 16色全書換でも 0.32ms。

**ビルドフラグの方針**: 実機で確認が取れた演出はフラグを持たず常時オン。ビルドフラグは
計測/切り分け用だけ(`DEBUG_PROF` / `DEBUG_FPS` / `NO_RAMX2` / `DEBUG_NOSEA` / `DEBUG_NOENT`)。

★メガクラッシュも**完了・常時オン**。B ボタンで発動(残数は画面下に棒の本数で表示)、スクロールも
BGM も止めて ①地鳴り(揺れ＋低いノイズ) → ②**手続き生成した稲妻**を3回(中点変位＋分岐 / 苦労と教訓 §11)
→ ③**画面幅の水の壁**が加速しながら駆け上がる → ④画面上の敵弾だけ消して BGM を**続きから**再開。
稲妻は V9938 の LINE。津波は **スプライト拡大(R#1 MAG)で1枚32x32** にした 8列×4段=32枚＝
256×128px(1走査線ちょうど8枚)を **列ごとに4px 下げて斜め**に置く(8枚制限を守れる傾きの上限)。絵は `tools/gen_wave.py` が生成し、焼く前にプレビューできる。
「止めてよい」と仕様で決めたので 30fps の予算に縛られない(クラッシュ中だけ 60fps で回す)。
openMSX(Z80機)実測で 3.8 秒。実機 turboR は CPU が数倍速いのでこれより短い。

★**TODO: 津波の多重化**（2026-09-13 要望）。**今と同じ大きさの波を2〜3個、続けて押し寄せさせる。**
   ★方針（ユーザー指定）: 波どうしは**十分に距離を取り、同じ走査線に2つの波が乗らない**ようにする
   ＝横8枚の制約はそのまま守る。薄くする・点滅させる、はしない。
   - 波1つの高さは 128＋傾き28＝156px。**次の波の上端は前の波の下端より下**（波の間隔 ≥ 156px＋余白）。
   - ★**枠の総数**: 波1つ32枚。画面(212行)に2つの波が一部ずつ同時に見える瞬間は、
     **見えている段だけ**スプライトを割り当てれば足りるか、足りなければ R#5 の帯切替(表セットB)で
     上の波と下の波を別の表に載せる。どちらになるかは間隔と画面上の段数を数えてから決める。
     セットC(下の残件)を使うなら VRAM 番地の取り合いに注意。
   - パターンは同じ波を使い回す（色は行ごとの色表で波ごとに変えて奥行きを出す余地あり）＝パターン枠は増やさない。
   - 敵弾消去は最後の波が抜けたときにまとめて行う。時間は伸びる(今 3.8 秒)ので、テンポは実機で見て決める。

残件: 3帯目(セットC=VRAM 0x6C00/0x6E00, R#5=0xDF)で表示枠を 24→36 へ。
★ただし page0 の 212〜220行(0x6A00〜0x6E7F)は宙返りの焼き置きが使っている。セットC の予定番地
(0x6C00=216行〜)と重なるので、着手するときはどちらかを移すこと。

5. **★A パレットエンジン**: 毎フレーム 16 色を計算で作る（被弾赤染め・稲妻フラッシュ・水面の
   カラーサイクル・ボス明滅）。VDP 帯域ほぼゼロで全画面が動く、最も費用対効果が高い一手。
6. ~~**メガクラッシュ**~~ **完了**: 稲妻(手続き生成)＋雷鳴＋敵弾全消し。
7. ~~**★B 衝撃波ディストーション**~~ **完了**: 砲台撃破/被弾の瞬間に、震源から上下へ広がる
   「縦のうねり」が走る。実装は `banked/ovl_shock.c`＋`core/raster.c`。
   ★**VRAM 転送ゼロ**。設計メモの「変位マップを計算して VDP コピーで提示」ではなく、
   **R#23 を走査線の途中で書き換えるだけ**で出す(§2-B ② の調査どおり)。実測で確定した天井は
   VDP 帯域なので、これは天井を完全に避けた演出。
   ★リングは震源の上下に1本ずつ、1本=2分割(帯の入口で外向きにずらし・出口で基準へ戻す)。
   ★分割は**間隔が狭いと発火しない**(割込み応答の遅れ。6行間隔で2本目以降が丸ごと出なかった)。
   帯幅16行・最小間隔12行。詳細と踏んだ罠は 苦労と教訓 §12。
   ★残件: 同じ仕組みで「沈没のうねり」「津波のあとの水面」も出せる(帯を置くだけ)。未着手。

### P2 — 看板ギミック

8. **★C アフィン変形ボス（回転＋拡縮）— 巨大 WWII 軍用機。第一候補 Douglas XB-19**

   ★**最終面(6面)として実装中（2026-09-13, v1）**。方式は「**全体スプライト＋MAG の帯分割**」（苦労と教訓 §15）:
   - 画面を HUD 帯(表A/等倍) / ボス帯(表B/MAG) / 自機帯(表A/等倍)に分け、境界に壁(MAG 8 枚)。自機は壁より上へ行けない。
   - ボス = 24 枚(2 枚重ねの OR 色あり)。コマ 27 枚は `tools/gen_boss.py` が三面図のシルエットから生成
     （登場: 等倍で旋回しながら降下→MAG に切替えて拡大 / 左右の傾き / 撃墜: きりもみしながら縮む）。
     VRAM page2 に焼き、コマ替えは HMMM 2 本。
   - 弾は背景に描く（海の模様を書き戻して消す）。自機弾・敵弾とも 1 ドット単位で動く。
   - BGM は「イントロ(576f)→本編ループ」。登場演出はイントロと同じ長さ（曲の開始時刻に合わせて進む）。
   - 弱点 = エンジン4＋銃座2（既存の砲台の当たり判定に相乗り。絵はボスのスプライト）。全部壊すと撃墜演出→結果→エンディング。
   ★コンセプト(ユーザー確定): **最終面は装備が初期に戻り、ボム無し、宙返りだけでボス1体を倒す**。他の敵は出さない。
   ★TODO(パワーアップを入れるとき): 最終面の開始(`stage_build` の `curstage == STAGE_FINAL` の分岐)で
     **武器を初期に戻し、ボム残数を0にする**処理を入れる。今はボム(`g_crush`)を0にするだけ(パワーアップ未実装)。
   ★v3(2026-09-13): コマ 63(登場26/傾き片側3段階/撃墜24/被弾で光る灰7)。ROM を 512KB に拡張(ボスのコマは bank32〜)。
     影(高度で離す・回転)、壁の影、被弾ごとの軽い音、コマ替えのダブルバッファ。
   ★残件: 弱点を壊した跡の見た目 / 撃墜時の爆発 / 難易度調整(弱点の耐久) / DEBUG_PROF ビルドの常駐残り 11B(次に常駐へ足す前に空けること) /
     オーバレイ(最終面)の残り 55B。

   ★**題材の狙い**: 「でっかい飛行機が回りながら迫る」＝turboR の見せ場。本作は海と戦艦の舞台なので、
   巨大機が海面／艦の上に覆いかぶさる絵が既存の資産（海・津波・R#23 のうねり）と同じ文脈に乗る。
   候補と選定理由は下の表。**機体そのものの防御砲座を潰して落とす**＝この作品の核をそのまま乗せる。

   ★**MULUW 検証スパイク: 完了（2026-09-13）**。結果は 苦労と教訓 §13。要点:
   - **sdasz80 は `mulub`/`muluw` のニモニックを知らない**（実測 "mnemonic error"）→ **生バイトで出す**。
     `MULUB A,r = ED (C1+8*r)` / `MULUW HL,BC = ED C3` / `MULUW HL,SP = ED F3`
     （[Z80/R800 instruction set, MSX Assembly Page](https://map.grauw.nl/resources/z80instr.php)）。
   - **意味を実測で確定**: `MULUW HL,BC` は **HL×BC → DE:HL（DE=上位 / HL=下位）**、
     `MULUB A,r` は **A×r → HL（A は保持）**。資料の要約が食い違っていたので測って決めた。
   - ★**ただし項目9 の前哨戦で分かったとおり、アフィンの内側ループに乗算は要らない**
     （加算だけで走る）。`MULUW` が効くのは係数作りや透視変換。回転/拡縮の本番でも
     支配的コストは画素の読み書き回数になる（苦労と教訓 §14-1）。
   - **速さ**: 16×16 の 1 回が **13.5µs（ハード）vs 110.8µs（SDCC の `__mulint`）= 8.2 倍**
     （オペランドはメモリ読み・コードはカートリッジ ROM 実行。openMSX の R800 実測なので
     実機で再確認のこと）。MULUW 側は ROM フェッチ律速なので、RAM 実行なら差はさらに開く。
   - **Z80 では ED の未定義オペコードが黙って無視される**＝答えだけ狂って落ちない。
     使う側は必ず `MSX_VER>=3`（＝R800）で分岐すること。
   - 検証は `DEBUG_PROF` の自己診断画面に常設した（`banked/prof_bank.c`）。実機でも読める。

   ★**実装の予算（§2-B ② の実測判定＋機体の縦横比で再計算）**:
   「事前生成コマを VRAM に置き VDP コマンドで貼る」が定石。G4 は 1 バイト = 2 ピクセルなので
   **横 2 倍は無料**＝半解像度で作れば表示は倍。
   ★★**巨大機はどれも「縦より横が長い」**（XB-19 は 1.60:1）。以前ここに「48×64（表示 96×128）」と
     書いたのは**縦長で誤り**だった。横長で取り直す（XB-19 = 表示 128×80 の場合）:
   - 64×40 実解像度のコマ = 1,280 B = 6.3 ms/frame → **30fps で可**。ただし画面の 1/4 幅で小さい
   - 表示 128×80（横 2px ブロック）= 64B/行 × 80 = 5,120 B = 25 ms/frame → 素のままでは 15fps 相当
   - ★**ダーティタイルだけ転送**すれば実効は落ちる。上面視の大型機は外接矩形の 35〜45% しか
     埋めない（細長い主翼＋胴体）ので、表示 128×80 でも実効 2.0〜2.3KB = 10〜11 ms
     ＝**30fps に入る**。ここが本命。
   回転そのもの＝事前生成コマ＋HMMM / 縦の変形（沈み込み・うねり）＝R#23 split（★B で実装済み）。

   ★**VRAM の置き場**: 128KB は使い切っているが、ゲーム中は **page0 の y32〜211 が空いている**
     （約 23KB。y0〜31 は火球ベイクが使用中）。面開始で焼く形なら足りる。
   ★**流用できるもの（項目9 で実装済み）**: 拡大＋縦圧縮の再標本化（`gen_planes.c` の `loop_prerender`）、
     「表示されない VRAM に焼いて VDP にコピーさせる」出し方、VRAM からの元絵取り出し `vdp_sprite_pattern_read()`。
     大型機では出力先がスプライトパターンから BG に変わる。面内回転は16x16では使えなかったので大型機で作り直す。

   ★**機体の選定（2026-09-13 調査）**。候補は「巨大な WWII 軍用機」。
   **決め手は寸法でも知名度でもなく「パブリックドメインの三面図があるか」**だった。

   | 機体 | 国・運用 | 全幅×全長 | 縦横比 | Commons の図面 | 判定 |
   |---|---|---|---|---|---|
   | **Douglas XB-19** | 米陸軍航空隊・試作重爆 | 64.62 × 40.34 m | 1.60 | **三面図2点＝PD-USGov-Military** | **★本命** |
   | Blohm & Voss BV 238 | 独・試作飛行艇 | 60.17 × 43.35 m | 1.39 | 図面あり＝PD-SDASM（弱め） | ★次点 |
   | Messerschmitt Me 323 | 独・実戦運用の輸送機 | （採用時に確認） | — | **無し**（写真のみ） | 図面が無い |
   | Hughes H-4 Hercules | 米・試作（1回飛行） | 97.51 × 66.65 m | 1.46 | **無し**（写真のみ） | 図面が無い |

   ★**Douglas XB-19 を第一候補にする理由**:
   - **三面図が PD-USGov-Military**。[Commons の3面図](https://commons.wikimedia.org/wiki/File:Douglas_XB-19_3-view_line_drawing_(technical_report).png)
     は **1942-07-18 の XB-19 技術報告書**（作者 Howard G. Bunker, Materiel Division, Air Corps,
     War Department）＝米連邦政府の職務著作。**戦艦の絵を起こしたときと同じ最強の根拠**で、
     「近代の作図者の著作物をトレースしてしまう」危険が無い。Commons に3面図が2点ある。
   - **爆撃機なので防御砲座を持つ**＝この作品の核（砲台を潰して落とす）がそのまま乗る。
     輸送機や飛行艇の「ただデカいだけの的」にならない。
   - 1.60:1 の横長。表示 128×80 なら 64B/行 × 80 = 5,120B = 25ms/frame ＝**ダーティタイル転送前提で 30fps**。
   - 実機は1機のみ製造・1949年に解体。**現存しない＝現代写真の権利問題も薄い。**

   ★**Blohm & Voss BV 238 を次点にする理由（演出との噛み合いは最良）**:
   - **飛行艇**＝海面から浮き上がって迫れる。本作の海・津波（メガクラッシュ）と同じ文脈に乗る。
   - **防御砲座22門**（13mm MG131×20、20mm MG151/20×2）＝「砲台を潰す」核と完全一致。6発。
   - 独機なので1面ボス（ビスマルク）と国籍が噛む。
   - ただし図面の根拠が [SDASM の PD-SDASM](https://commons.wikimedia.org/wiki/File:Bv_238_43933197.jpg)
     ＝「no known restrictions」で、**PD-USGov-Military より弱い**（原図は独企業の1944年もの）。
     採るなら1枚ごとに出典と根拠を控えること。

   ★**Me 323 / H-4 は「図面が無い」**。魅力はあるが、寸法（事実）＋PD写真から自力で上面視を
   描き起こす手間と、形の正確さのリスクを負う。上2機で足りるなら選ばない。
   - H-4 の寸法: 全幅 319ft11in (97.51m) / 全長 218ft8in (66.65m) / 8 × P&W R-4360 /
     4翅 Hamilton Standard 直径 5.23m / 契約 1942 / 初飛行 1947-11-02。
   - 使える H-4 写真: [SDASM Archives の Flickr](https://www.flickr.com/photos/sdasmarchives/7881222436/)
     — "No known copyright restrictions"（Catalog 01_00091696）。
   - **使わない**: Smithsonian NASM のアルバム（NASM.1997.0017）は Terms of Use で許諾要。

   ★**資料の扱い（必ず守る）**: 図面を起こすときは**その図面ごとに出典 URL と PD の根拠**を
   `docs/` に控える。米連邦政府の職務著作は PD だが、**近代の作図者による三面図はその人の著作物**。
   メガクラッシュの稲光で著作権を主張しているストックフォトを使いかけて止めた前例がある
   （苦労と教訓 §11-1、§13-7）。

9. ~~**宙返り回避**~~ **完了（2026-09-13）** ― 1943 とは「解釈違い」の宙返り。
   実装は `banked/ovl_rot.c`（アフィン）＋`core/player.c`＋`core/scene_stage.c`。

   ★**操作**: **A＋B**（撃ちながら B）で発動。**B 単押し**（撃っていない）がメガクラッシュ。
     実戦では指はほぼ常に A を押しているので、**撃ちっぱなしの指のまま反射で出せる**。
     ★当初 B＋上にしたが却下。**上は移動なので、避けたい場面で突っ込む操作になる**（苦労と教訓 §14-6）。
   ★**1943 との違い（意図して変えた点）**:
   - **(a) 機体が本当に変形する**。1942 系列は固定の数コマだが、こちらはアフィン変形で
     **上昇するほど大きく（頂点＝背面で2倍）、見かけの縦寸が縮んで真横（一本線）になり、背面に入って戻る**。
     影は高度ぶん海面側へ離れる。2×2 の4スプライト合成で最大 32×32。
     ★コマは面の準備で bank19 が page0 の表示されない行（212〜220）に9コマ焼き、宙返り中は1行を HMMM で
     パターン表へコピーするだけ＝**フレーム時間は通常と同一**（苦労と教訓 §14-3c）。
     ★パターン枠は津波の枠を借りる＝新しい枠は消費しない。
   - **(b) 回数制ではなくクールダウン制**（`LOOP_CD`＝1.5秒）。パニックボタンではなく避けのリズム。
   - **(c) 無敵ではなく「面から外れる」**。弾は下を抜けるが、**回っている間は撃てない**。
   - **(d) 抜けに ★B の衝撃波リング**が小さく出る（自分の演出資産と繋げた）。
   ★宙返りは 1942 系列の専売特許ではない（[Sky Kid（ナムコ, 1985）](https://en.wikipedia.org/wiki/Sky_Kid)が
     独立した代表ムーブとして持つ）。ゲームの仕組み自体は著作権の保護対象ではない（守られるのは
     絵・音・コード・固有名などの表現と、名前/ロゴの商標）。

   ★★**前哨戦で判明したこと（項目8 に直結。詳細は 苦労と教訓 §14）**:
   - **アフィンの内側ループに乗算は 1 個も要らない**（逆写像＋二重増分＝加算だけ。行頭もシフトで作れる）。
     ＝ §13 で測った `MULUW` の 8.2 倍は**内側ループには効かない**。効くのは係数作りと透視のような変換。
     **大型機でも支配的コストは画素の読み書き回数**＝ダーティタイル転送の設計が本命という結論を補強。
   - **16x16 の面内回転は中間角で読めない**（アルゴリズムは正しく 0/90/180/270 は綺麗。
     特徴が 1〜3px だと 22.5 度でエイリアスして潰れる。元絵を太らせても駄目）。
     **面内回転が効くのは特徴が 10px 以上ある大きな絵**＝大型機（表示 128×80）なら問題ない。
   - **上から見た宙返りは回転ではなく「縦の圧縮」**＝対角行列＝**拡大縮小そのもの**。
     `loop_prerender()` の倍率表を `cos` から「迫ってくる係数」に差し替えれば**大型機へ流用できる**（面内回転版 `ovl_rot_frame`/`ovl_rot_squash` は削除済み、git 15f09e9 に残る）。
     前哨戦という見立ては当たっていたが、当たっていたのは**回転ではなく拡縮の方**だった。

10. CPU 弾幕（D）／数千パーティクル（E）。LUT 用に ROM 512KB 拡張（`rompack.mjs` ＋ `config.mk`）。

### P2-B — 1〜5面のてこ入れ(2026-09-13 着手)

案(ユーザーに提示): A1 編隊ウェーブ＋POW＋時限武器 / A2 道中の中型目標 / A3 対艦ボム / B1 撃沈シーンの刷新 /
B2 面ごとの時間帯・天候 / B3 雲の層 / B4 中型機(最終面の驚きを残すため 2×2 まで) / C 固有攻撃の予告。
1. ~~**常駐の空き作り**~~ **完了**: 結果画面と開始カードの文字を bank19 へ。DEBUG_PROF の常駐残り 11B→828B。
2. ~~**B2 面ごとの時間帯・天候**~~ **完了(v1)**: パレットだけで 1面=昼/2面=夕焼け/3面=荒天(ときどき稲光と雷鳴)/
   4面=朝霧/5面=夜戦。表は `tools/gen_grade.py`(スクショを塗り直す見本も出せる)。弾/爆発/数字(11,12,15)は全面で同じ明るさ、
   暗い面では自機と敵機の色を持ち上げて読めるようにした。カード/結果画面では昼の色へ戻す。最終面は昼。
3. ~~**B1 撃沈シーンの刷新**~~ **完了(v1)**: 見本は 1943(見下ろし視点の沈没＝船尾から水に沈み、艦が船首へ向かって短くなって消える)。
   撃破の瞬間に撃沈専用の RAM オーバレイ(`ovl_sink.c`, bank28)へ入れ替え、誘爆→水没(水面より下を海の模様で上書き、
   すぐ上は海の色を網目で重ねる、線に白波と爆発、画面はゆっくり前へ)→静まる→結果画面。書くのは表示リングと艦バッファ B の両方。
   割れて波紋が広がる案は「波の再利用で沈没らしくない」と却下。結果画面へ移る瞬間に開始カードが見えた不具合(09ae8a1 で混入)も修正。
   実機の指摘で追加修正: 沈んだ砲身/停泊機のスプライトを消す / 船首に泡が残る / 終わり際に網目の帯で砲塔のドームが浮く /
   結果画面へ移る瞬間にずれた艦の帯が見える / 最終面の結果画面とエンディングにボスの表 B と MAG が残る(raster_off で基準へ戻す)。
4. ~~B3 雲の層~~ **取りやめ**(低空飛行は雲を描かないための設計。試作の網目雲は却下)。次: B4 大型機の編隊(中ボス)。B4 と A 系は相談しながら。

### P3 — アイデンティティ

11. 名称・タイトル画・`仕様書.md` の書き換え、CHANGELOG / VERSION 運用の開始。

---

## 2-B. 大型演出 3 案の実現可能性（実測定数で判定・2026-09-11）

判定の土台は [性能と高速化 §0-0](性能と高速化.md) の実測定数
（**VDP 204 B/ms・残枠 3.9 KB/frame**）。**MSX コミュニティの一次情報に当たった結果、
当初「帯域的に不可能」と判断した 3 案は、いずれも前提が誤っていた**ので記録を残す。

### ① 縦の多重スクロール → **可能（ただし「左右」ではなく「レイヤ」）**

- **誤っていた前提**: 「毎フレーム全画面を再合成しなければならない(27KB/frame)」。
  実際は **償却できる**。[artrag/Parallax_scrolling_MSX2](https://github.com/artrag/Parallax_scrolling_MSX2)
  は screen5 で 2 層パララックス＋8方向スムーススクロールを**素の MSX2(Z80 3.58MHz)**で実現している。
- **機構**: 画面を 16×16 ブロックで構成し、**16 フレームかけて 1 列ずつ裏ページに再構築** →
  完成したら表裏入替。その間の 1px の動きは HW スクロール(R#23/R#26-27)が担う。
  背景は前景の **15/16 速度**（16 はスクロールレジスタのステップ数由来）。スプライトは全部ゲーム用に空く。
  VRAM 4 ページ使用（2=ダブルバッファ / 2=両レイヤのブロック素材）。
- **コスト**: 27KB ÷ 16 = **1.7 KB/frame = 8.3 ms**。残枠 3.9KB に収まる。現行の海+艦描画
  (2.9KB/frame)と**置き換える**形なので差し引きでは軽くなりうる。
- **障壁は帯域でなく VRAM 設計**: 本作は page0=スプライト/炎, page1=表示リング, page2-3=艦バッファB で
  128KB を使い切っている。艦を「ブロックの集合」として持ち直す改造が要る。
- **残る本当の制約**: **画面を縦に割って左右で別速度は不可**（R#23 は画面全体に効く単一レジスタ。
  community も "A clean split of the screen in left and right parts is not possible on V9938/V9958"）。
  横帯に割る screen split は可能。

### ② 戦艦級の疑似回転・拡大縮小 → **サイズで線が引ける**

- **誤っていた前提**: 「R800 が毎ピクセル計算 → VRAM へ転送」という形だけを検討していた。
  MSX2 の定石は **事前生成コマを VRAM に置き、VDP コマンドで貼る**。screen5 は 4 ページ=128KB ある。
- G4 の「1 バイト = 2 ピクセル」で**横 2 倍は無料**（半解像度で作れば表示は倍）。

| 対象 | バイト数 | 所要 | 判定 |
|---|---|---|---|
| 48×64 のコマ | 1,536 B | 7.5 ms/frame | 30fps で可 |
| 64×100（表示 128×200） | 3,200 B | 15.7 ms/frame | 15fps なら可 |
| 戦艦級 128×200 実解像度 | 12,800 B | 63 ms/frame | **不可（2fps 相当）** |

- ★**見落としていた強い手: [screen split で R#23 を行ごとに変える](https://map.grauw.nl/articles/split_guide.php)**。
  R#23 は **mid-frame 変更が即時反映される非シームレス・レジスタ**＝走査線ごとに「VRAM のどの行を
  表示するか」を選べる。**縦方向の拡大縮小・うねり・傾き・沈み込みが VRAM 転送ゼロで作れる**
  （設計メモ §2-B の全画面ディストーションの相当部分が、コピーでなくレジスタ書換で実現できる）。
  - 作法: R#23 を split で変えたら **R#19 を再計算**（split 行は画面行でなく VRAM 行 0 起点で数える）。
    変更前に E1(R#0 bit4)を落として S#1 を読む。
  - 制約: HBLANK 中に書けるのは **レジスタ 4 本（間接アクセスなら 8 本）＋パレット 8 エントリ**。
    212 行全部で割り込むと CPU をほぼ食い潰す（split guide が警告）→ **帯単位に限定するのが現実的**。
- **役割分担の結論**: 回転そのもの＝事前生成コマ＋HMMM / 縦の変形＝R#23 split。

### ③ スプライト限界を超える弾数 → **可能。3 案で最も安い**

- **誤っていた前提**: 「32 枚はハード上限だから終わり」。**回避技法が確立している**。
- **スプライト分割(sprite split / line interrupt)**: R#19 に分割行、S#1 の FH を見る（または R#0 の
  E1 で割り込み）。分割行で SAT を書き換え**同じ 32 枚を別の帯で再利用**。2 箇所分割で **48 枚**、
  分割を増やせばさらに。**Space Manbow が実ゲームで使用**。
- **別解**: SAT を VRAM に 2 つ持ち R#5/R#11 を切替（正順・逆順で並べ実効 16 枚/走査線をフレーム交互）。
- **併用**: 1 パターンに複数弾を描くクラスタパターン（`run_fire` の RING/AIMFAN は元々編隊で出る）。
- **コスト**: SAT 全書換 128B = 0.64ms。2 分割で 3 セット使っても **1.9 ms/frame**。
  クラスタパターン 8 枚再生成 = 256B = 1.3ms。→ 合計 3ms 程度で **48〜96 発相当**。
- **残る本当の制約**: **1 走査線 8 枚はハード制約で回避不可**。横一線の壁は出せない。
  → 弾幕は**縦に散る形（リング・螺旋・雨）**に寄せる。
- 弾の**計算**は純 RAM 演算で VDP コスト 0＝「重い計算・軽い出力」の理想形。**制約は表示だけ**。

### 一次情報（次に読むときの入口）

- [Screensplit programming guide (Grauw)](https://map.grauw.nl/articles/split_guide.php) ― split の作法の決定版
- [A guide to scrolling game engines on MSX (Grauw)](https://map.grauw.nl/articles/scrolling.php)
- [V9938 VRAM timings (Grauw/openMSX)](https://map.grauw.nl/articles/vdp-vram-timing/vdp-timing.html) ― コマンド単価の実機計測
- [artrag/Parallax_scrolling_MSX2](https://github.com/artrag/Parallax_scrolling_MSX2) ― パララックスの動く実装
- [More than 32 sprites on 9918/9938](https://www.msx.org/forum/msx-talk/development/more-than-32-sprites-on-99189938) / [Sprite split](https://www.msx.org/forum/msx-talk/development/sprite-split-0)
- [VRAM access timing (MSX Game Library)](https://aoineko.org/msxgl/index.php?title=VRAM_access_timing)
- ★[openMSX issue #2057](https://github.com/openMSX/openMSX/issues/2057): **openMSX 21 は VDP コマンドが
  実機より速い(未解決)**。帯域の絶対値をエミュで測ってはいけない。実機の自己診断が唯一の正解。

---

## 3. この計画で守る規律

- **性能の主張は実機計測とセットでのみ書く**（§1-3）。openMSX の数値は正しさの検証にのみ使う。
- **「何を計算するか」より「何を VRAM に書かないか」**（設計メモ §3）。演出の出力は
  パレット・スクロール・少数スプライト・低頻度/小領域の VDP コマンドに押し込む。
- **失敗と見送りも残す**（前作 `性能と高速化.md` §3-D の方式）。効かなかった最適化こそ次への資産。
